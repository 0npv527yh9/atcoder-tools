# FEATURES

今後追加したい機能リストです。未完了の機能は `- [ ]` で残し、完了した機能は `Done` セクションへ移動します。実装順の目安として、短期・中期・長期に分けています。

## 短期

- [ ] `submit` コマンドを実装する。
- [ ] `cookie check` の保存処理と表示を整理する。
- [ ] `fetch-test --live` を追加し、ログイン状態や最新 HTML を前提にした取得を明示できるようにする。
- [ ] `test` の実行入力を stdin とファイル入力から選べるようにする。

## 中期

- [ ] AtCoder の提出結果を確認できるようにする。
- [ ] テストケースを手動で追加・削除・一覧表示できるようにする。
- [ ] 複数言語設定を改善し、設定ミスの検出を強くする。
- [ ] コンテスト単位でタスク情報を一覧表示できるようにする。
- [ ] `fetch-test` 後にそのままテンプレート生成や初回テストへつなげる導線を作る。
- [ ] タスクページをブラウザで開くコマンドを追加する。
- [ ] タスクページの内容を Markdown として保存できるようにする。

## 長期

- [ ] contest 初期化コマンドを追加する。
- [ ] 解答テンプレート生成を追加する。
- [ ] watch mode を追加し、保存時に自動でテストできるようにする。
- [ ] AtCoder Problems など外部情報との連携を検討する。
- [ ] 過去提出や外部情報から参考コードを取得する機能を検討する。
- [ ] 次のコンテストや次のタスクを開く導線を検討する。
- [ ] CI やローカル開発向けに、実 HTTP を使わない検証モードを整える。

AtCoder Problems などの外部サービス連携は便利だが、まずは AtCoder 本体のログイン、取得、テスト、提出の流れを安定させてから検討する。

## Done

- [x] cli でのログイン機能を廃止し、代わりに cookie 文字列を受け取りログイン状態にできるようにする
  - cli でのログインはブロックされるようになった
  - ブラウザでログイン後、cookie を連携する
