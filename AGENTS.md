# AGENTS.md

このリポジトリは Rust 製の AtCoder 用 CLI ツールです。AtCoder へのログイン、サンプルテスト取得、ローカルテスト、将来的な提出を扱います。

## 基本方針

- サンプルコードを示す場合は Rust で書く。
- 既存の型安全性を尊重する。特に `Url<PageType>` や `Html<PageType>` のようなページ種別付き型は維持する。
- クリーンアーキテクチャを目指すが、層を増やすこと自体を目的にしない。
- 副作用を app 層から外し、ユースケースをテストしやすい形へ寄せる。
- 実 HTTP、実ファイル IO、実端末入力、`process::exit` は薄い境界に閉じ込める。
- 古い scratch TODO から拾う場合は、完了済みメモや迷いだけの項目をそのまま戻さず、現在の `TODO.md` / `FEATURES.md` に合う粒度へ整理する。

## 確認コマンド

基本の確認は次を使う。

```sh
cargo test
```

必要に応じて、個別テストや ignored テストを明示的に実行する。ただし、実 HTTP や実ログインが必要なテストは通常の確認対象に含めない。

## 設計上の注意

- `process::exit` は `main` 付近に閉じ込める。`app` の関数はできるだけ `Result` や終了状態を返す。
- `unwrap_or_exit` をユースケース内部に広げない。
- `domain` は `handler` に依存させない。ドメインは値の表現や純粋な判定に寄せる。
- IO と表示は、純粋な変換や判定から分ける。
- 実 HTTP テストと HTML fixture ベースの parser テストを分ける。
- fake/mock ベースで app 層のテストを書ける構造を優先する。
- ログイン時は CSRF token を新規取得する。保存済み token の再利用を前提にしない。
- HTML parser は失敗を黙って捨てすぎない。欠落や構造変更を検知できる戻り値やエラーを優先する。
- 相対パスはどのディレクトリ基準かを明確にする。`set_current_dir` への依存を増やさない。
- `anyhow` は探索中の一時利用なら許容するが、CLI の主要経路では意味のある error enum に寄せる。

## 作業時の注意

- ユーザーが行った変更を勝手に戻さない。
- 大きなリファクタリングでは、まず小さくテスト可能な境界を作る。
- 既存の public な CLI 体験は、明示的な理由がない限り壊さない。
- `submit` は未実装なので、README の通常利用例には含めない。
